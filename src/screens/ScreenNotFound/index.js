import React from 'react';

import './index.scss';

const ScreenNotFound = (props) => {
    return (
        <div className='screen-not-found'>
            Page Not Found
        </div>
    );
};

export default ScreenNotFound;
